import { React } from 'react';
import MinAndMaxDisplay from './minAndMaxDisplay';

const PriceMatrix = () =>
	<div className="price-matrix"> { MinAndMaxDisplay() } </div>;

export default PriceMatrix;
